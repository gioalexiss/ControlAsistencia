spring.application.name=controlAsistencia
server.port=${PORT:8085}
spring.datasource.url=jdbc:mysql://mysql-32a884e7-practi.l.aivencloud.com:20421/defaultdb?useSSL=true
spring.datasource.username=avnadmin
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#Puerto - Normalmente trabaja en el 8080 - Servidor WEB
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Configuraci�n de correo para Gmail
# IMPORTANTE: En Render, configura estas variables de entorno:
# MAIL_USERNAME - Tu correo de Gmail
# MAIL_PASSWORD - Contrase�a de aplicaci�n de Gmail (NO tu contrase�a normal)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=${MAIL_TIMEOUT:10000}
spring.mail.properties.mail.smtp.timeout=${MAIL_TIMEOUT:10000}
spring.mail.properties.mail.smtp.writetimeout=${MAIL_TIMEOUT:10000}
# Configuraci�n adicional importante
spring.mail.protocol=smtp
spring.mail.test-connection=false
spring.mail.default-encoding=UTF-8
# Logging para debug
logging.level.org.springframework.mail=${MAIL_LOG_LEVEL:INFO}
logging.level.com.sun.mail=${MAIL_LOG_LEVEL:INFO}
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.cache=false
spring.thymeleaf.mode=HTML